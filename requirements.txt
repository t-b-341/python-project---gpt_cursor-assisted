pygame>=2.0.0
pytest>=7.0.0
# Optional dependencies:
moderngl>=5.0.0  # optional, required for GPU shader pipeline and shader test/settings scenes
# Optional: For JIT compilation alternative to C extension
# numba>=0.56.0
